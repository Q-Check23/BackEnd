## 디스코드 OAuth2
- /auth/login으로 로그인 요청 -> 디스코드 로그인 링크(환경변수 처리)로 리다이렉트
- /auth/code로 인가코드 받음
- 인가코드로 토큰 발급 후, 토큰으로 닉네임과 이메일 가져옴
- 이메일로 기존 회원 여부 확인
- 기존 회원이면 userId로 JWT access 토큰, refresh 토큰 발급 => access는 헤더에, refresh는 쿠키에 넣어서 전달
- 신규 회원이면 닉네임, 이메일로 회원가입용 JWT 발급